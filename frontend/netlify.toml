# Netlify alternative to Vercel. Base directory: frontend
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: send every route to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
